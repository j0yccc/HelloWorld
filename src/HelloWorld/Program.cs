using System;
using System.Collections.Generic;

namespace HelloWorld
{
    public class Program
    {
        static void Main(string[] args)
        {/*
            var helloWorld = new HelloWorld();

            Console.WriteLine($"{helloWorld.HelloWorldString()} is working");

            //Reverse string
            var originalString = "abcdef";
            var reversedString = helloWorld.ReverseString(originalString);

            Console.WriteLine($"{originalString} reversed string is {reversedString}");

            //Remove duplicate string
            var originalString1 = "AAAGGGTTT";
            var removedDuplicateString = helloWorld.RemoveDuplicateString(originalString1);

            Console.WriteLine($"{originalString1} removed duplicate string is {removedDuplicateString}");

            //Two sum
            Tuple<int, int> indices = TwoSum.FindTwoSum(new List<int>() { 3, 1, 5, 7, 5, 9 }, 10);
            if (indices != null)
            {
                Console.WriteLine(indices.Item1 + " " + indices.Item2);
            }
            
            //User Input
            UserInput.TextInput input = new UserInput.NumericInput();
            input.Add('1');
            input.Add('a');
            input.Add('0');
            Console.WriteLine(input.GetValue());
            
            //Merge Names
            string[] names1 = new string[] {"Ava", "Emma", "Olivia"};
            string[] names2 = new string[] {"Olivia", "Sophia", "Emma"};
            Console.WriteLine(string.Join(", ", MergeNames.UniqueNames(names1, names2)));

            //Binary Search Tree
            Node n1 = new Node(1, null, null);
            Node n3 = new Node(3, null, null);
            Node n2 = new Node(2, n1, n3);

            Console.WriteLine(BinarySearchTree.Contains(n2, 3));*/

            //var helloWorld = new HelloWorld();
            //helloWorld.RotateLeft(new int[] {1,2,3,4,5});
            // ProgrammerTeacher teacher = new ProgrammerTeacher();
            // teacher.AddLanguage("C#");

            // Programmer programmer = new Programmer();
            // teacher.Teach(programmer, "C#");

            // foreach (var language in programmer.Languages)
            // {
            //     Console.WriteLine(language);
            // }
        }
    }
}
